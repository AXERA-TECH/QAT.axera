# torch 2.10 依赖(requirements.txt = torch 2.6 基线)
# 装法见 README_2_10.md「安装依赖」:PyTorch 栈走官方源定 CUDA,其余走清华源

# --- PyTorch 栈(不绑 +cuXXX,CUDA 由 --index-url 决定)---
torch==2.10.0
torchvision==0.25.0
torchaudio==2.10.0
torchao==0.16.0            # 2.10 的 torch.ao PT2E 已坏,QAT 必须走 torchao

# --- ONNX 生态(与 torch 2.10 官方 CI pin 对齐)---
onnx==1.19.1
onnxscript==0.6.2          # 修 optimize 折叠权重 DQ 链
onnx-ir==0.1.15            # 需 <0.1.16
onnxruntime==1.23.2
onnx-graphsurgeon==0.6.1
onnxslim==0.1.94

# --- 其他 ---
tqdm==4.68.4
pyyaml==6.0.3
ipython==9.15.0
